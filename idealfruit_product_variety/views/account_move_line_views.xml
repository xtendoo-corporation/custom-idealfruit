<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_move_line_form_inherit" model="ir.ui.view">
        <field name="name">account.move.line.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='name']" position="after">
                <field name="move_type" invisible="1"/>
                <field name="product_variety_id" attrs="{'required': [('move_type', '=', 'in_invoice')]}" string="Variedad" domain="[('id', 'in', product_variety_available_ids)]"/>
                <field name="product_variety_available_ids" string="Variedades Disponibles" widget="many2many_tags" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>
